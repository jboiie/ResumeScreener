# ──────────────────────────────────────────────────────────────
# Resume Screener — Python Dependencies
# torch (CPU-only) is installed before this file in the Dockerfile
# ──────────────────────────────────────────────────────────────

# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Embedding model
sentence-transformers>=3.0.0

# Vector database client
qdrant-client>=1.9.0

# Document parsing
pdfplumber>=0.11.0
python-docx>=1.1.0

# LLM rerankers (both installed; only the configured one is called at runtime)
groq>=0.9.0
google-generativeai>=0.7.0

# HTTP utilities
httpx>=0.27.0
